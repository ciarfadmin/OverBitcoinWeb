function MainCtrl($scope) {

	//var mainLoginPanel = $('#mainLoginPanel');//JQuery request for the app pane element.
	//mainLoginPanel.html(mainLoginPanel);//The dynamically loaded data
	//$compile(mainLoginPanel.contents())($scope);//Tells Angular to recompile the contents of the app pane.
}